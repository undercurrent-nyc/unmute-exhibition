<div class="
  flex flex-row h-56
  {{if 
    (eq @place "acfny") 
      "justify-start" 
      "justify-end"
  }}"
  {{did-insert this.addDots }}
  >
  {{svg-jar (concat @place "-map") 
    class=(concat
      (concat @place "-map")
      " h-56")}}
</div>
  {{#each @data as |modal|}}
   {{#if (eq this.showingModal modal.id)}}
      {{#modal-dialog 
        onClose=this.closeModal
        translucentOverlay=true
        }}
        <h1>{{modal.title}}</h1>
        {{#each modal.artists as |artist|}}
          <p>{{artist}}</p>
        {{/each}}
        <button {{on "click" this.closeModal}}>Close</button>
      {{/modal-dialog}}
    {{/if}}
  {{/each}}
